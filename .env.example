# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# DASHSCOPE_API_KEY: Required for Alibaba DashScope / Qwen API calls.
# Users configure this via the Secrets panel in the AI Studio UI to enable real Alibaba Qwen models.
# Make sure to input your actual API Key starting with 'sk-', not the endpoint URL.
DASHSCOPE_API_KEY="MY_DASHSCOPE_API_KEY"

# DASHSCOPE_API_BASE: Optional custom regional/workspace API endpoint for Alibaba Model Studio.
# e.g., ws-xxxxxxxxxxxxxxxxx.cn-beijing.maas.aliyuncs.com
DASHSCOPE_API_BASE=""

# Tencent CloudBase Configuration (for cloud database storage)
CLOUDBASE_SECRET_ID="YOUR_TENCENT_CLOUDBASE_SECRET_ID"
CLOUDBASE_SECRET_KEY="YOUR_TENCENT_CLOUDBASE_SECRET_KEY"
CLOUDBASE_ENV_ID="YOUR_TENCENT_CLOUDBASE_ENV_ID"

